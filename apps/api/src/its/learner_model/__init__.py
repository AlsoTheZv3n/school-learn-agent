"""The open learner model: an inspectable per-skill mastery estimate.

P3: the LEARNER MODEL improves, not the agent. The agent's behavior is a (largely
deterministic) function of this model, so behavior stays auditable. BKT (bkt.py) is
interpretable (P5); DKT (dkt.py) is a deliberate later swap.
"""
