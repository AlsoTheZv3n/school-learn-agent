# Explicit project name so this stack never collides with another project's
# default "infra" name (the folder is called infra/). See: cancer-research-prototype-v2.
name: school-learn-agent

services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: its
      POSTGRES_PASSWORD: its_dev_pw
      POSTGRES_DB: its
    ports:
      # Host 5433 (not 5432): a native PostgreSQL service may already own 5432 on
      # dev machines. Bound to localhost only. Container-internal port stays 5432.
      - "127.0.0.1:5433:5432"
    volumes:
      - its_pgdata:/var/lib/postgresql/data
      - ./init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U its -d its"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  its_pgdata:
